/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.pexels.com', 'raw.githubusercontent.com', 'camo.githubusercontent.com'],
  },
};

export default nextConfig;